# Switch-it サイト

作業を始める前に、必ず `PROJECT.md` を読んでください。
このプロジェクトの背景、デザインの決めごと、文章を書く際の方針、
過去の失敗例などがまとめてあります。

## 特に守ってほしいこと

- **専門用語を使わない**。依頼者はプログラミングが専門外です。
  「ヒーローセクション」ではなく「トップページの一番上の大きな部分」のように説明してください。
- **CSSを編集したら、`PROJECT.md` 7章の確認スクリプトを実行**してください。
  過去に、CSSへの追記が空振りしてスタイルが当たらない不具合を出しています。
- **商品の説明文は、Creemaの文章をそのまま使わない**でください。
  検索エンジンに重複コンテンツと判断されるため、必ず書き直します。
- **写真を追加したら圧縮**してください(長辺1000px、JPEG品質82)。
  過去に画像が20MBまで膨らみ、表示が重くなったことがあります。

## よく使うコマンド

```bash
# ローカルで表示を確認する（ブラウザで http://localhost:8000 を開く）
python3 -m http.server 8000

# 全ページの検証（画像・リンク・タグ・未定義クラス）
# → PROJECT.md の7章にスクリプトあり
```

## 触ってはいけないもの

- `images/items/` の写真は圧縮済みです。再圧縮を繰り返すと画質が落ちます
- `css/style.css` は1ファイルで全ページを賄っています。分割しないでください
